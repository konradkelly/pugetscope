VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3001/live

# Public VAPID key for browser push alerts — see websocket/.env.example.
# Blank disables the Alerts panel's push flow client-side.
VITE_VAPID_PUBLIC_KEY=
