PORT=3000
NODE_ENV=development

DATABASE_URL=postgres://pugetscope:pugetscope@localhost:5433/pugetscope
REDIS_URL=redis://localhost:6379

CORS_ORIGIN=http://localhost:5173

# Optional — blank disables real password-reset emails, logging the reset
# link to stdout instead (see api/src/email/sendPasswordResetEmail.ts).
SES_FROM_EMAIL=
